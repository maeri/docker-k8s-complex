apiVersion: v1
kind: Service
metadata:
  name: worker-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: worker
  ports:
    # external port -> from outside of pod
  - port: 1111
    # internal port -> inside of pod
    targetPort: 1111
    name: http
