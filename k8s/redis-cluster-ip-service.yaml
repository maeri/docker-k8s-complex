apiVersion: v1
kind: Service
metadata:
  name: redis-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: redis
  ports:
    # external port -> from outside of pod
  - port: 6379
    # internal port -> inside of pod
    targetPort: 6379
    name: redis
